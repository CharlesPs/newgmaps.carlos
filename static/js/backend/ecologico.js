function setup_ecologico(){
	//$("#form-save").on("submit", distritos_update);
}

function ecologico_update(e){
	/*e.preventDefault();

	var data = {};

	data.entry = $("#entry").val();
	data.nombre = $.trim($("#nombre").val());
	data.color = $.trim($("#color").val());

	$.ajax({
		url : 'admin/distritos/save',
		data : data,
		type : 'post',
		error : function(){},
		success : function(ret){
			if(ret == "1"){
				bootstrap_alert("<strong>Hecho!</strong> El registro se actualizo con éxito.", "success");
			}else{
				bootstrap_alert("<strong>Upps!</strong> El registro no pudo actualizarse.", "error");
			}

		}
	});*/
}